machine:
  java:
    version: openjdk7

test:
  override:
    - mvn test:
        timeout: 600
    - rm -rf $CIRCLE_ARTIFACTS && mkdir $CIRCLE_ARTIFACTS
    - for mod in quickload-*; do test -d $mod/target/site/jacoco && mv $mod/target/site/jacoco $CIRCLE_ARTIFACTS/`basename $mod`; done || echo ok
